# Calibres

## Status

| ID  | Feature | Description courte | Lot | État |
|-----|---------|--------------------|-----|------|
| C01 | Liste calibres configurable | Liste simple dans config.yaml (priorité config.local.yaml) | A | fait |
| C02 | Autocomplétion sans autoremplacement | Saisie assistée : suggestions sélectionnables, sans écrasement de la saisie | A | en cours (NT-073) |
| C03 | Saisie libre | Toute valeur peut être saisie directement, sans option générique `Autre` | A | en cours (NT-073) |
| C04 | MAJ formulaires sessions | Appliquer aide sur: prévues (création/édition), wizard, réalisées (création/édition) | A | fait |
| C05 | Préférence calibre par défaut | Paramètre utilisateur (Hive) pour préremplir les nouvelles sessions uniquement | B | en cours (NT-073) |
| C06 | Résolution statistique | Regrouper les seuls alias connus sans réécrire l’historique ni les nouvelles saisies | A | en cours (NT-073) |
| C07 | Tests ciblés | Autocomplétion, cas ambigus, préremplissage par préférence | B | fait |

## Calibres harmonisés

1. Harmoniser la liste des calibres
Le champ "calibre" doit rester libre (string) mais l'app doit guider l'utilsiateur et faciliter la saisie.
    - Création d'une liste de calibres habituels
    - Cette liste doit être facilement configurable (config.yaml)
    - En mode saisie, cette liste est proposée à l'utilisateur, qui peut choisir un calibre de la liste pour remplir le champ "calibre"
    - Autocomplétion sur le champ
        - Exemple 1 : si l'utilisateur remplit "22", on propose ".22 LR"
        - Exemple 2 : si l'utilisateur remplit "45", on propose ".45 ACP"
    - Liste des calibres : .22 LR, .32 S&W Long, 9mm (9x19), .38 Special, .357 Magnum, .380 ACP, .40 S&W, .45 ACP
    - Une valeur libre reste possible ; aucune entrée générique `Autre` n'est proposée.

2. Formulaires à mettre à jour :
    - Sessions prévues : création et édition
    - Sessions prévues : wizard
    - Sessions réalisées : création et édition

## Préférences : choisir un calibre par défaut

1. Autoriser l'utilisateur à choisir un calibre par défaut dans les paramètres (dans la même section que la Prise 1 main/2 mains)

2. Si cette préférence est non vide, elle préremplit les nouvelles sessions réalisées et prévues. L'édition et le wizard conservent la valeur enregistrée.
3. Les alias `9mm`, `9 mm`, `9x19`, `9 mm Para` et `9mm (9x19)` sont regroupés sous `9 mm` dans les statistiques par calibre, sans modifier les données. `.380 ACP` reste distinct ; `9 mm court` n'est pas un alias.
