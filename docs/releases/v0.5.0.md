# NexTarget (app) v0.5.0 — 2026-07-09

Release des sprints S1 (Sécurité & Qualité) et S2 (Demo-ready) — beta demo FFTir.

## Sécurité (S1)
- **NT-061** — Coach « connecté uniquement » : suppression de l'appel Mistral
  direct et de toute clé API côté client (y compris dans les builds APK).
  Sans compte : message clair + bouton de connexion. Le carnet de tir reste
  100 % hors-ligne.
- **NT-051** — `flutter_lints` actif, zéro issue (138 corrigées, dont un bug
  réel de routing `/settings`) ; garde CI `flutter analyze --fatal-infos`.

## Demo-ready (S2)
- **NT-075** — Onboarding 3 écrans au premier lancement (revoir via
  Paramètres > Aide) + aide contextuelle « ? » sur Sessions, Objectifs,
  Exercices.
- **NT-032** — Ton du coach (Neutre / Cool) sélectionnable dans
  Paramètres > Coach IA.
- Bouton **+** de « Mes sessions » aligné sur l'onglet actif (réalisée/prévue),
  appui long supprimé (retour de recette).

## Compatibilité
- Données locales Hive : **aucune migration nécessaire** (schéma inchangé ;
  nouvelles préférences additives : `onboarding_seen`, `coach_persona`).
- Nécessite NexTarget-server ≥ 0.2.0 pour le ton « Cool » (sinon le défaut
  neutre s'applique) ; l'analyse coach requiert désormais un compte Google.

## Build de démo
`./scripts/build_apk.sh` (plus aucune clé à injecter).

Tests : 224 verts + 2 skips, analyze zéro issue. Détail : `CHANGELOG.md`.
